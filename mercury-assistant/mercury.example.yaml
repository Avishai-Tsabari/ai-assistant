# Rename to mercury.yaml to use. Secrets stay in .env only.
# See mercury-fork/docs/configuration.md for precedence (env overrides YAML).

# server:
#   port: 8787
#   bot_username: mercury

# model:
#   chain:
#     - provider: mistral
#       model: mistral-large-latest
#     - provider: google
#       model: gemini-2.5-flash
#     - provider: groq
#       model: llama-3.3-70b-versatile
#     - provider: openrouter
#       model: meta-llama/llama-3.3-70b-instruct
#   provider: anthropic                   # single-provider shorthand (no chain)
#   model: claude-sonnet-4-6              # single-model shorthand
#   fallback_provider: google             # fallback if primary fails
#   fallback: gemini-2.5-flash            # fallback model
#   max_retries_per_leg: 2                # retries per chain leg (0–5)
#   chain_budget_ms: 55000                # total time budget across all legs (ms)
#   capabilities:                         # arbitrary capability flags passed to model
#     key: value

# trigger:
#   patterns: "@Mercury,Mercury,@bishi_assistant_bot,bishi_assistant_bot"
#   match: all                            # how patterns are matched

# compaction:
#   keep_recent_tokens: 2000

# conditional_context:
#   enabled: true
#   classifier: llm

# ingress:
#   whatsapp: false
#   slack: false
#   teams: false
#   discord: false
#   telegram: true

# runtime:
#   data_dir: .mercury
#   auth_path: .mercury/auth              # path to auth storage
#   whatsapp_auth_dir: .mercury/whatsapp  # path to WhatsApp auth dir
#   max_concurrency: 2
#   log_level: info                       # debug | info | warn | error | silent
#   log_format: text                      # text | json
#   rate_limit_per_user: 10               # max requests per window per user
#   rate_limit_window_ms: 60000           # rate limit window duration (ms)

# agent:
#   image: mercury-agent:latest
#   container_timeout_ms: 300000          # container kill timeout (10000–3600000 ms)
#   container_bwrap_docker_compat: true

# discord:
#   gateway_duration_ms: 300000           # gateway session duration (60000–3600000 ms)

# telegram:
#   format_enabled: true                  # enable Telegram message formatting

# media:
#   enabled: true
#   max_size_mb: 10                       # max upload size in MB (1–100)

# permissions:
#   admins: "telegram:YOUR_TELEGRAM_USER_ID"
