# Extensions

Optional capabilities are installed here as subfolders (each with `index.ts`).

- Use the Mercury dashboard **Features** tab, or
- Run `mercury add <source>` (local path, `npm:…`, `git:…`, or GitHub shorthand).

Restart Mercury after adding or removing an extension.
